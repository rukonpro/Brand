import React from 'react';
import Categories from "@/app/(admin)/admin/dashboard/categories/page";

const CategoriesLayout = ({children}) => {

    return (
        <div>
            {children}
        </div>
    );
};

export default CategoriesLayout;